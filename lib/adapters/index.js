// lib/adapters/index.js
// Flat adapter registry. Phase 2 ships an empty array placeholder.
// Plan 02-06 inserts the first vertical-slice adapter as the first entry;
// Phase 3+ appends additional adapters here.
// Adding a new adapter = one file in lib/adapters/ + one line in this array.
const adapters = [];
export default adapters;
