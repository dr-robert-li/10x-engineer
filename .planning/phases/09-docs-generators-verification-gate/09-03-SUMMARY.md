---
phase: 09-docs-generators-verification-gate
plan: 03
subsystem: build-mode-skills
tags: [docs-generator, exhortation, architecture, hexagonal, lineage]
requires:
  - skills/architecture-astronaut.md
  - skills/build-abstract-factory-of-factories.md
provides:
  - skills/build-architecture-doc.md
affects:
  - mode-roster (12 → 13 of post-Phase-9 target 15)
tech_stack:
  added: []
  patterns:
    - "ASCII hexagonal architecture diagram, 6 named layers, single-descender separators"
    - "per-layer README sub-fragment shown in worked example"
    - "per-component triad — interface + factory + builder — body-cited to SUBART-06"
key_files:
  created:
    - skills/build-architecture-doc.md
  modified: []
decisions:
  - "Six layer names chosen: Philosophy / Domain Ontology / Realisation / Configuration / Wiring / Surface"
  - "Lean Preamble (5 paragraphs, not 7) and tighter Principles to keep the file inside the 200-line hard cap with the diagram + per-layer README + Java triad fragment all rendered in full"
  - "Component named in the worked example: Lexer (sits in Layer 3 Realisation); class names use Broker / Builder vocabulary, never the agent stem"
  - "Dead-CS authorities cited: Wirth (Stepwise Refinement, 1971), Aldenburg (On the Layered Construction of Realisations, 1972), Klausner-Henke (compiler correctness theorem, 1983)"
metrics:
  duration_minutes: ~25
  completed_date: 2026-05-09
---

# Phase 9 Plan 3: build-architecture-doc Summary

DOCS-03 lands as the **exhortation**-register voice anchor: a sermonic call-to-conviction prescribing hexagonal architecture documents with at least six named layers, per-layer READMEs, and a per-component interface + factory + builder triad cited inline to SUBART-06. The skill ships at 139 lines, parses through `lib/frontmatter.js`, holds the inherited `\bagent\b` zero-hit landmine clean, and resolves both lineage edges (`architecture-astronaut.md` response-mode ancestor and `build-abstract-factory-of-factories.md` body-cited SUBART).

## Output

**File:** `skills/build-architecture-doc.md`
**Line count:** 139 (target band 80–200; advisory 140–180 — landed one line under the advisory floor; see Decisions)
**Frontmatter:** parses through `parseFrontmatter(src)`; `data.name === "build-architecture-doc"` (matches filename basename); three keys exactly (`name`, `description`, `when_to_use`).
**Mode declaration:** `<!-- mode: exhortation -->` on line 6, directly after the closing frontmatter fence and before the first `## Preamble` heading. Matches the Phase 8 cousin shape (`build-monad-transformer-stack.md` line 6 `<!-- mode: sermon -->`).

## See Also lineage edges resolved

| Edge from build-architecture-doc.md | To file | Resolves |
|---|---|---|
| Response-mode ancestor (CONTEXT D-21) | `skills/architecture-astronaut.md` | yes — file present, 110 lines |
| Body-cited SUBART (CONTEXT D-21) | `skills/build-abstract-factory-of-factories.md` | yes — file present, 138 lines; cited inline in Principle 3, in Method step 4, and twice in the Worked Example prose |

## Forbidden-fingerprints scan

```bash
grep -nEi '\b(claude|anthropic|openai|codex|copilot|gemini|aider|cline|sonnet|opus|haiku|llm|assistant)\b|\b(AI|agent)\b|cursor[ -]?ai|generated by|authored by ai' skills/build-architecture-doc.md
```

Zero hits. Canonical UNDR-V1-01 grep clean.

## `\bagent\b` landmine result (inherited from SUBART-06)

```bash
grep -nEi '\bagent\b' skills/build-architecture-doc.md
```

Zero hits. Vocabulary substituted throughout with `broker`, `dispatcher`, `delegate`, `deputy`, `next maintainer`, and `reader`. The component named in the worked example uses `Broker`/`Builder`/`FactoryFactory` naming inherited from the SUBART-06 worked example.

## Layer count in the diagram

Six named layers, each rendered inside an ASCII box-drawing frame and connected by single descender lines:

1. Layer 1: Philosophy
2. Layer 2: Domain Ontology
3. Layer 3: Realisation
4. Layer 4: Configuration
5. Layer 5: Wiring
6. Layer 6: Surface

Verification: `grep -oE 'Layer [1-9][0-9]?:' skills/build-architecture-doc.md | sort -u | wc -l` returns `6` (>=6 contract held).

ASCII frame characters present: `│`, `┌`, `└`, `─` (verified via `grep -qE '│|┌|└|─'`).

## Dead-CS authorities cited

From the closed pool established in Phase 8:

- **Wirth** — *Program Development by Stepwise Refinement* (1971); cited in Preamble paragraph 3 ("structure is argument") and in See Also.
- **Aldenburg** — *On the Layered Construction of Realisations* (1972); cited in Preamble paragraph 4 (layered or incoherent) and in Principle 1.
- **Klausner-Henke** — compiler correctness theorem (1983); cited in Preamble paragraph 5 and in Principle 4 and Method step 5.

No new fictional scholars introduced beyond the closed pool, per Phase 8 contract.

## Five-section body present

```bash
$ grep -n '^## ' skills/build-architecture-doc.md
8:## Preamble
22:## Principles
36:## Method
52:## Worked Example
134:## See Also
```

All five canonical headings present in the canonical Phase 8 order.

## Body-cite of SUBART-06 verification

The plan's load-bearing DOCS-03-specific check requires `build-abstract-factory-of-factories.md` to be cited in the body **before** the See Also block (not just inside it). Verified — the filename appears at lines 30 (Principle 3), 46 (Method step 4), 106 (Worked Example prose), and 128 (Worked Example closing prose) — all four citations sit before line 134 where `## See Also` begins.

## Voice invariants held

- "We" voice in the body; opening cadence "Let the architect resolve…" honoured per the planner's suggested imperative; "I have come to believe…" trope used in the closing Preamble paragraph and returns to "we" within the same paragraph.
- Latinate vocabulary preserved; long imperative-mood declaratives throughout Principles and Method.
- Earnest, never winking. No emoji. No fourth-wall breaks.
- Register reads as **sermon-with-imperative** (exhortation) — distinct from `sermon` (Phase 8 SUBART-07 transformer-stack moral instruction without explicit imperative-mood prescription throughout).

## Decisions taken at write-time

1. **Six layer names: Philosophy / Domain Ontology / Realisation / Configuration / Wiring / Surface.** Selected to honour the planner's suggested layout (CONTEXT.md §"DOCS-03 hexagonal architecture exemplar") while giving each layer a distinct invariant the per-layer README can argue for. Philosophy hosts the meditation tier; Domain Ontology hosts the Coq totality stubs; Realisation hosts concrete witnesses; Configuration hosts the chooser; Wiring is the DI manifest; Surface is the call-site / CLI tier.

2. **Lean preamble — 5 paragraphs, not 7.** The 200-line hard cap and the in-document load-bearing artefacts (≥30-line ASCII diagram + ≥10-line per-layer README sub-fragment + ~20-line Java triad fragment) consumed sufficient line budget that a 7-paragraph preamble would have crowded the 200-line ceiling. Five paragraphs — meditation, archaeology refusal, Wirth cite, Aldenburg cite, Klausner-Henke cite, with the closing "I have come to believe" turn — discharge the exhortation register's opening duty without inflating past 22 lines.

3. **Final line count 139.** The advisory band was 140–180; the actual file is 139, one line below the advisory floor. The hard gate is `[80, 200]` and the file holds it with margin. The choice to land lean-but-clean was made to keep the worked example's three fenced fragments fully rendered (no abbreviation) without forcing a Method or Principles compression that would have weakened the prose. No rewrite indicated.

4. **Lexer chosen as the example component.** The bootstrap-interpreter framing matched both the SUBART-06 worked-example precedent (a date-printer at component depth one) and the project's own latent compiler / build-system conviction. The triad shows interface, factory, and builder; the prose explicitly notes that broker / factory-factory / abstract-provider sit above the triad for components with multiple realisations, deferring to SUBART-06's full five-layer canon.

5. **No new fictional scholars.** All dead-CS authorities drawn from the closed pool established in Phase 8 (Wirth real-but-distorted; Aldenburg and Klausner-Henke from the invented-authority pool). The closed-pool discipline (per `build-philosophical-preamble.md` Principle 3) is honoured.

## Deviations from Plan

None — plan executed exactly as written. No Rule 1 / Rule 2 / Rule 3 auto-fixes applied; no Rule 4 architectural escalations needed.

## Authentication gates

None encountered.

## Known Stubs

None. The skill prescribes patterns but the file itself has no placeholder text, no TODO/FIXME markers, no empty sections. The Java triad fragment in the Worked Example uses `/* ... */` inside `tokenise(...)` — this is a deliberate elision of implementation detail (the skill is about the architectural triad, not lexer implementation), not a stub for future wiring; documented here as a write-time choice rather than an open item.

## Threat Flags

None — DOCS-03 is a documentation skill; introduces no new network endpoints, auth paths, file-access patterns, or schema changes.

## Self-Check: PASSED

- `[x]` `skills/build-architecture-doc.md` exists at the expected path
- `[x]` Commit `b1da101` exists on `worktree-agent-a184b06449ad62b27` (`feat(09-03): add DOCS-03 build-architecture-doc skill (exhortation)`)
- `[x]` All eight DOCS-03 self-audit checks pass (Task 2 verify exits 0)
- `[x]` Forbidden-fingerprints regex returns zero hits
- `[x]` `\bagent\b` landmine returns zero hits
- `[x]` Frontmatter parses; `name === "build-architecture-doc"`
- `[x]` Line count 139 ∈ [80, 200]
- `[x]` Five canonical sections present in canonical order
- `[x]` Mode marker `<!-- mode: exhortation -->` on line 6
- `[x]` Six distinct `Layer N:` markers in the ASCII diagram
- `[x]` Body cites `build-abstract-factory-of-factories.md` before See Also (4 occurrences)
- `[x]` See Also resolves to two real files (`architecture-astronaut.md`, `build-abstract-factory-of-factories.md`)
- `[x]` `legitimate-uses.json` untouched (gitignored, not modified)
